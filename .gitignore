# rust / anchor
target/
**/target/
Cargo.lock

# node
node_modules/
app/node_modules/
app/.next/
*.tsbuildinfo

# local cluster state
magicblock-test-storage/
test-ledger/
.logs/
.stack.pids
mb-stack.log
vrf-oracle*.log

# secrets — never commit a funded key
keys/
.seed-village.json
# seed.ts writes the village authority keypair here on every run. It is output,
# never input, and it is a private key: it must never be committed.
.seed-village-authority.json
.env.devnet
app/.env.local

# vendored reference examples (cloned by scripts/setup.sh)
vendor/

.DS_Store

# fetched by the app build, not authored here
!app/public/fonts/
